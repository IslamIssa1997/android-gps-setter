<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Real overlay permission, used by the joystick and the floating map window. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Required from Android 13 (API 33); without it the location notification and the
         navigation foreground service notification are silently dropped. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The target apps picker needs to enumerate installed packages. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPSSetter"
        tools:targetApi="34">
        <activity
            android:name=".ui.TargetAppsActivity"
            android:exported="false"
            android:label="@string/setting_target_apps"
            android:parentActivityName=".ui.ActivitySettings" />
        <activity
            android:name=".ui.ActivitySettings"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:launchMode="singleTop">
        </activity>
        <activity
            android:name=".ui.MapActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Disabled until the user turns on external control in settings; ControlReceiver
             flips it at runtime. -->
        <receiver
            android:name=".control.ControlReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="com.islam.gps.action.START" />
                <action android:name="com.islam.gps.action.STOP" />
                <action android:name="com.islam.gps.action.SET_LOCATION" />
            </intent-filter>
        </receiver>

        <!-- Prunes an uninstalled app from the target list immediately. PACKAGE_FULLY_REMOVED is a
             protected, system-only broadcast, so exporting it is safe and it only fires on a real
             uninstall. -->
        <receiver
            android:name=".control.PackageRemovedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <service
            android:name=".utils.JoystickService"
            android:exported="false" />

        <service
            android:name=".utils.FloatingMapService"
            android:exported="false" />

        <service
            android:name=".utils.NavigationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Required for AppCompatDelegate.setApplicationLocales to persist and auto-restore the
             chosen language across restarts. Without it the language picker only applied for the
             current session and the app came back in the system language on next launch. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>